<h1>Create a new list</h1>

<%= form_with model: @list do |f| %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <%= f.submit %>
<% end %>

<%= link_to "Back to lists", lists_path %>
